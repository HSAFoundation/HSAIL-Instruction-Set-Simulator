#!/bin/csh

set path = ($path $HOME/bin)

set OUTFILE = $HOME/mcw/log/hg-sync.log
set TOOLSDIR = external/amd/tools/sync-mcw-and-amd

cd $HOME
cd mcw/simulator/sync-tree/hsail2brigamd

touch $OUTFILE
date "+DATE: %Y%m%d-%H%M%S" | tee -a $OUTFILE
$TOOLSDIR/sync-mcw-and-amd | tee -a $OUTFILE
